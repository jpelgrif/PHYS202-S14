#!/bin/bash

# This script configures git whenever you start working
# on multiple machines. Run it any time you decide to work
# with git on a new computer

git config --global user.name "John Pelgrift"
git config --global user.email "jpelgrif@calpoly.edu"
git config --global color.ui "auto"
git config --global core.editor "nano"
